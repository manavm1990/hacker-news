# The 'contract' for client-server communication 💬
type Query {
  # Fetch all
  feed: [Link!]!

  # Fetch a single link by its `id`
  link(id: ID!): Link
}

type Mutation {
  post(url: String!, description: String!): Link!
}

type Link {
  id: ID!
  description: String!
  url: String!
}
